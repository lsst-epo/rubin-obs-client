# Factories

These factories will match the handle of a block in our Craft CMS content blocks and return the corresponding React component. 

Dynamic components typically contain data that is manipulated live (filtering, sorting) or fetched live (not statically rendered).