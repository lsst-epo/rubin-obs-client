import { type Meta, type StoryObj } from "@storybook/react";

import IconGroup from ".";

const meta: Meta<typeof IconGroup> = {
  component: IconGroup,
};
export default meta;

export const Primary: StoryObj<typeof IconGroup> = {
  args: {},
};
