import { type Meta, type StoryObj } from "@storybook/react";

import GalaxyDistance from ".";

const meta: Meta<typeof GalaxyDistance> = {
  component: GalaxyDistance,
};
export default meta;

export const Primary: StoryObj<typeof GalaxyDistance> = {
  args: {},
};
