import * as Styled from "./styles";
import PropTypes from "prop-types";

Styled.WidgetGrid.displayName = "Layout.WidgetGrid";

Styled.WidgetGrid.propTypes = {
  children: PropTypes.node,
};

export default Styled.WidgetGrid;
