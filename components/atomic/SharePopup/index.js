import PropTypes from "prop-types";

function SharePopup({ title, url }) {
  return null;
}

SharePopup.displayName = "Atomic.SharePopup";

SharePopup.propTypes = {
  title: PropTypes.string,
  url: PropTypes.string,
};

export default SharePopup;
